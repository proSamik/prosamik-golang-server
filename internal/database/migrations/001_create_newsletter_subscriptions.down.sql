-- Drop the `newsletter_subscriptions` table if it exists
DROP TABLE IF EXISTS newsletter_subscriptions;

-- Drop the sequence for the `id` field
DROP SEQUENCE IF EXISTS newsletter_subscriptions_id_seq;
